import { lazy } from "react";

export const Navigation = lazy(() => import("./Navigation"));
export const Portfolio = lazy(() => import("./My-Portfolio"));
export const About = lazy(() => import("./About-Me"));
export const PageNotFound = lazy(() => import('./PageNotFound'));
